# Nixpacks — Build config para Coolify / Railway
# Docs: https://nixpacks.com/docs

[phases.setup]
  nixPkgs = ["nodejs"]
  cmds = ["corepack enable"]

[phases.build]
  cmds = ["npm run build"]

[start]
  cmd = "npm start"
